import os
import time

from dust.core import project

find_timestamped_file(directory, pattern):
    """ Find timestamped files in a directory
    """
    # pattern: "foo{}bar"


if __name__ == '__main__':
    proj = project.load_project()
    
    
 
